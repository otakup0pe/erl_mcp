{erl_opts, [debug_info, warnings_as_errors]}.

{deps, [
    {cowboy, "2.12.0"}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info, nowarn_export_all]}
    ]}
]}.

{project_plugins, [
    rebar3_hex,
    rebar3_ex_doc
]}.

{shell, [{apps, [erl_mcp]}]}.
